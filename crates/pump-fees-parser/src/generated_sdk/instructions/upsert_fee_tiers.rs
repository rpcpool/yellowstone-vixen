//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::generated::types::FeeTier;
use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const UPSERT_FEE_TIERS_DISCRIMINATOR: [u8; 8] = [227, 23, 150, 12, 77, 86, 94, 4];

/// Accounts.
#[derive(Debug)]
pub struct UpsertFeeTiers {
    pub fee_config: solana_pubkey::Pubkey,

    pub admin: solana_pubkey::Pubkey,

    pub config_program_id: solana_pubkey::Pubkey,

    pub event_authority: solana_pubkey::Pubkey,

    pub program: solana_pubkey::Pubkey,
}

impl UpsertFeeTiers {
    pub fn instruction(
        &self,
        args: UpsertFeeTiersInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: UpsertFeeTiersInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.fee_config, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.admin, true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.config_program_id,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.event_authority,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = UpsertFeeTiersInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::PUMP_FEES_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpsertFeeTiersInstructionData {
    discriminator: [u8; 8],
}

impl UpsertFeeTiersInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [227, 23, 150, 12, 77, 86, 94, 4],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for UpsertFeeTiersInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpsertFeeTiersInstructionArgs {
    pub fee_tiers: Vec<FeeTier>,
    pub offset: u8,
}

impl UpsertFeeTiersInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `UpsertFeeTiers`.
///
/// ### Accounts:
///
///   0. `[writable]` fee_config
///   1. `[signer]` admin
///   2. `[]` config_program_id
///   3. `[]` event_authority
///   4. `[]` program
#[derive(Clone, Debug, Default)]
pub struct UpsertFeeTiersBuilder {
    fee_config: Option<solana_pubkey::Pubkey>,
    admin: Option<solana_pubkey::Pubkey>,
    config_program_id: Option<solana_pubkey::Pubkey>,
    event_authority: Option<solana_pubkey::Pubkey>,
    program: Option<solana_pubkey::Pubkey>,
    fee_tiers: Option<Vec<FeeTier>>,
    offset: Option<u8>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl UpsertFeeTiersBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn fee_config(&mut self, fee_config: solana_pubkey::Pubkey) -> &mut Self {
        self.fee_config = Some(fee_config);
        self
    }
    #[inline(always)]
    pub fn admin(&mut self, admin: solana_pubkey::Pubkey) -> &mut Self {
        self.admin = Some(admin);
        self
    }
    #[inline(always)]
    pub fn config_program_id(&mut self, config_program_id: solana_pubkey::Pubkey) -> &mut Self {
        self.config_program_id = Some(config_program_id);
        self
    }
    #[inline(always)]
    pub fn event_authority(&mut self, event_authority: solana_pubkey::Pubkey) -> &mut Self {
        self.event_authority = Some(event_authority);
        self
    }
    #[inline(always)]
    pub fn program(&mut self, program: solana_pubkey::Pubkey) -> &mut Self {
        self.program = Some(program);
        self
    }
    #[inline(always)]
    pub fn fee_tiers(&mut self, fee_tiers: Vec<FeeTier>) -> &mut Self {
        self.fee_tiers = Some(fee_tiers);
        self
    }
    #[inline(always)]
    pub fn offset(&mut self, offset: u8) -> &mut Self {
        self.offset = Some(offset);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = UpsertFeeTiers {
            fee_config: self.fee_config.expect("fee_config is not set"),
            admin: self.admin.expect("admin is not set"),
            config_program_id: self
                .config_program_id
                .expect("config_program_id is not set"),
            event_authority: self.event_authority.expect("event_authority is not set"),
            program: self.program.expect("program is not set"),
        };
        let args = UpsertFeeTiersInstructionArgs {
            fee_tiers: self.fee_tiers.clone().expect("fee_tiers is not set"),
            offset: self.offset.clone().expect("offset is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `upsert_fee_tiers` CPI accounts.
pub struct UpsertFeeTiersCpiAccounts<'a, 'b> {
    pub fee_config: &'b solana_account_info::AccountInfo<'a>,

    pub admin: &'b solana_account_info::AccountInfo<'a>,

    pub config_program_id: &'b solana_account_info::AccountInfo<'a>,

    pub event_authority: &'b solana_account_info::AccountInfo<'a>,

    pub program: &'b solana_account_info::AccountInfo<'a>,
}

/// `upsert_fee_tiers` CPI instruction.
pub struct UpsertFeeTiersCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub fee_config: &'b solana_account_info::AccountInfo<'a>,

    pub admin: &'b solana_account_info::AccountInfo<'a>,

    pub config_program_id: &'b solana_account_info::AccountInfo<'a>,

    pub event_authority: &'b solana_account_info::AccountInfo<'a>,

    pub program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: UpsertFeeTiersInstructionArgs,
}

impl<'a, 'b> UpsertFeeTiersCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: UpsertFeeTiersCpiAccounts<'a, 'b>,
        args: UpsertFeeTiersInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            fee_config: accounts.fee_config,
            admin: accounts.admin,
            config_program_id: accounts.config_program_id,
            event_authority: accounts.event_authority,
            program: accounts.program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.fee_config.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.admin.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.config_program_id.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.event_authority.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = UpsertFeeTiersInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::PUMP_FEES_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(6 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.fee_config.clone());
        account_infos.push(self.admin.clone());
        account_infos.push(self.config_program_id.clone());
        account_infos.push(self.event_authority.clone());
        account_infos.push(self.program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `UpsertFeeTiers` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` fee_config
///   1. `[signer]` admin
///   2. `[]` config_program_id
///   3. `[]` event_authority
///   4. `[]` program
#[derive(Clone, Debug)]
pub struct UpsertFeeTiersCpiBuilder<'a, 'b> {
    instruction: Box<UpsertFeeTiersCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> UpsertFeeTiersCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(UpsertFeeTiersCpiBuilderInstruction {
            __program: program,
            fee_config: None,
            admin: None,
            config_program_id: None,
            event_authority: None,
            program: None,
            fee_tiers: None,
            offset: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn fee_config(
        &mut self,
        fee_config: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.fee_config = Some(fee_config);
        self
    }
    #[inline(always)]
    pub fn admin(&mut self, admin: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.admin = Some(admin);
        self
    }
    #[inline(always)]
    pub fn config_program_id(
        &mut self,
        config_program_id: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.config_program_id = Some(config_program_id);
        self
    }
    #[inline(always)]
    pub fn event_authority(
        &mut self,
        event_authority: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.event_authority = Some(event_authority);
        self
    }
    #[inline(always)]
    pub fn program(&mut self, program: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.program = Some(program);
        self
    }
    #[inline(always)]
    pub fn fee_tiers(&mut self, fee_tiers: Vec<FeeTier>) -> &mut Self {
        self.instruction.fee_tiers = Some(fee_tiers);
        self
    }
    #[inline(always)]
    pub fn offset(&mut self, offset: u8) -> &mut Self {
        self.instruction.offset = Some(offset);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = UpsertFeeTiersInstructionArgs {
            fee_tiers: self
                .instruction
                .fee_tiers
                .clone()
                .expect("fee_tiers is not set"),
            offset: self.instruction.offset.clone().expect("offset is not set"),
        };
        let instruction = UpsertFeeTiersCpi {
            __program: self.instruction.__program,

            fee_config: self.instruction.fee_config.expect("fee_config is not set"),

            admin: self.instruction.admin.expect("admin is not set"),

            config_program_id: self
                .instruction
                .config_program_id
                .expect("config_program_id is not set"),

            event_authority: self
                .instruction
                .event_authority
                .expect("event_authority is not set"),

            program: self.instruction.program.expect("program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct UpsertFeeTiersCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    fee_config: Option<&'b solana_account_info::AccountInfo<'a>>,
    admin: Option<&'b solana_account_info::AccountInfo<'a>>,
    config_program_id: Option<&'b solana_account_info::AccountInfo<'a>>,
    event_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    program: Option<&'b solana_account_info::AccountInfo<'a>>,
    fee_tiers: Option<Vec<FeeTier>>,
    offset: Option<u8>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
