//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use solana_pubkey::Pubkey;
use borsh::BorshSerialize;
use borsh::BorshDeserialize;


#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct RandomnessState {
pub discriminator: [u8; 8],
/// Hash chain heads (default: 3 chains)
pub chain_heads: [[u8; 32]; 3],
/// Active entropy queue (processed in current block)
pub active_queue: Vec<[u8; 32]>,
/// Pending entropy queue (will become active in next block)
pub pending_queue: Vec<[u8; 32]>,
/// Steps executed in current block
pub steps_in_current_block: u32,
/// Maximum steps allowed per block
pub max_steps_per_block: u32,
/// Total steps executed across all blocks
pub total_steps: u64,
/// Last slot when tick was called (for block transition detection)
pub last_tick_slot: u64,
/// Global admin authority (migrated from GlobalEntropy)
#[cfg_attr(feature = "serde", serde(with = "serde_with::As::<serde_with::DisplayFromStr>"))]
pub admin: Pubkey,
/// List of trusted entropy providers (migrated from GlobalEntropy)
#[cfg_attr(feature = "serde", serde(with = "serde_with::As::<Vec<serde_with::DisplayFromStr>>"))]
pub trusted_providers: Vec<Pubkey>,
/// List of authorized readers (migrated from GlobalEntropy)
#[cfg_attr(feature = "serde", serde(with = "serde_with::As::<Vec<serde_with::DisplayFromStr>>"))]
pub authorized_readers: Vec<Pubkey>,
/// Last update timestamp
pub last_updated: i64,
/// Total number of entropy updates
pub update_count: u64,
/// Bump seed for PDA
pub bump: u8,
/// Mining difficulty parameter for randomness chain
pub mining_difficulty: u64,
/// Current height of the randomness chain (publicly readable)
pub randomness_chain_height: u64,
/// Circular buffer storing recent mined randomness values
pub randomness_chain_buffer: [[u8; 32]; 20],
/// Current tip/head of the randomness chain
pub randomness_chain_head: [u8; 32],
/// Incrementing nonce included in all head updates
pub nonce: u64,
}


pub const RANDOMNESS_STATE_DISCRIMINATOR: [u8; 8] = [162, 226, 24, 69, 173, 182, 21, 48];

impl RandomnessState {
  
  
  
  #[inline(always)]
  pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
    let mut data = data;
    Self::deserialize(&mut data)
  }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for RandomnessState {
  type Error = std::io::Error;

  fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
      let mut data: &[u8] = &(*account_info.data).borrow();
      Self::deserialize(&mut data)
  }
}

#[cfg(feature = "fetch")]
pub fn fetch_randomness_state(
  rpc: &solana_client::rpc_client::RpcClient,
  address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::DecodedAccount<RandomnessState>, std::io::Error> {
  let accounts = fetch_all_randomness_state(rpc, &[*address])?;
  Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_randomness_state(
  rpc: &solana_client::rpc_client::RpcClient,
  addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::DecodedAccount<RandomnessState>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<RandomnessState>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      let account = accounts[i].as_ref()
        .ok_or(std::io::Error::new(std::io::ErrorKind::Other, format!("Account not found: {}", address)))?;
      let data = RandomnessState::from_bytes(&account.data)?;
      decoded_accounts.push(crate::shared::DecodedAccount { address, account: account.clone(), data });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_randomness_state(
  rpc: &solana_client::rpc_client::RpcClient,
  address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::MaybeAccount<RandomnessState>, std::io::Error> {
    let accounts = fetch_all_maybe_randomness_state(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_randomness_state(
  rpc: &solana_client::rpc_client::RpcClient,
  addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::MaybeAccount<RandomnessState>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<RandomnessState>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      if let Some(account) = accounts[i].as_ref() {
        let data = RandomnessState::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::MaybeAccount::Exists(crate::shared::DecodedAccount { address, account: account.clone(), data }));
      } else {
        decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
      }
    }
  Ok(decoded_accounts)
}

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountDeserialize for RandomnessState {
      fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
      }
  }

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountSerialize for RandomnessState {}

  #[cfg(feature = "anchor")]
  impl anchor_lang::Owner for RandomnessState {
      fn owner() -> Pubkey {
        crate::SETTLEMENT_ID
      }
  }

  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::IdlBuild for RandomnessState {}

  
  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::Discriminator for RandomnessState {
    const DISCRIMINATOR: [u8; 8] = [0; 8];
  }

