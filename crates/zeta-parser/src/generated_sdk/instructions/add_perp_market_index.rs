//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::{BorshDeserialize, BorshSerialize};

use crate::generated::types::Asset;

/// Accounts.
#[derive(Debug)]
pub struct AddPerpMarketIndex {
    pub market_indexes: solana_program::pubkey::Pubkey,

    pub pricing: solana_program::pubkey::Pubkey,
}

impl AddPerpMarketIndex {
    pub fn instruction(
        &self,
        args: AddPerpMarketIndexInstructionArgs,
    ) -> solana_program::instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }

    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: AddPerpMarketIndexInstructionArgs,
        remaining_accounts: &[solana_program::instruction::AccountMeta],
    ) -> solana_program::instruction::Instruction {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_program::instruction::AccountMeta::new(
            self.market_indexes,
            false,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new(
            self.pricing,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = borsh::to_vec(&AddPerpMarketIndexInstructionData::new()).unwrap();
        let mut args = borsh::to_vec(&args).unwrap();
        data.append(&mut args);

        solana_program::instruction::Instruction {
            program_id: crate::ZETA_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct AddPerpMarketIndexInstructionData {
    discriminator: [u8; 8],
}

impl AddPerpMarketIndexInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [122, 40, 14, 64, 169, 18, 231, 136],
        }
    }
}

impl Default for AddPerpMarketIndexInstructionData {
    fn default() -> Self { Self::new() }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct AddPerpMarketIndexInstructionArgs {
    pub asset: Asset,
}

/// Instruction builder for `AddPerpMarketIndex`.
///
/// ### Accounts:
///
///   0. `[writable]` market_indexes
///   1. `[writable]` pricing
#[derive(Clone, Debug, Default)]
pub struct AddPerpMarketIndexBuilder {
    market_indexes: Option<solana_program::pubkey::Pubkey>,
    pricing: Option<solana_program::pubkey::Pubkey>,
    asset: Option<Asset>,
    __remaining_accounts: Vec<solana_program::instruction::AccountMeta>,
}

impl AddPerpMarketIndexBuilder {
    pub fn new() -> Self { Self::default() }

    #[inline(always)]
    pub fn market_indexes(&mut self, market_indexes: solana_program::pubkey::Pubkey) -> &mut Self {
        self.market_indexes = Some(market_indexes);
        self
    }

    #[inline(always)]
    pub fn pricing(&mut self, pricing: solana_program::pubkey::Pubkey) -> &mut Self {
        self.pricing = Some(pricing);
        self
    }

    #[inline(always)]
    pub fn asset(&mut self, asset: Asset) -> &mut Self {
        self.asset = Some(asset);
        self
    }

    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: solana_program::instruction::AccountMeta,
    ) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }

    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_program::instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }

    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_program::instruction::Instruction {
        let accounts = AddPerpMarketIndex {
            market_indexes: self.market_indexes.expect("market_indexes is not set"),
            pricing: self.pricing.expect("pricing is not set"),
        };
        let args = AddPerpMarketIndexInstructionArgs {
            asset: self.asset.clone().expect("asset is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `add_perp_market_index` CPI accounts.
pub struct AddPerpMarketIndexCpiAccounts<'a, 'b> {
    pub market_indexes: &'b solana_program::account_info::AccountInfo<'a>,

    pub pricing: &'b solana_program::account_info::AccountInfo<'a>,
}

/// `add_perp_market_index` CPI instruction.
pub struct AddPerpMarketIndexCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_program::account_info::AccountInfo<'a>,

    pub market_indexes: &'b solana_program::account_info::AccountInfo<'a>,

    pub pricing: &'b solana_program::account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: AddPerpMarketIndexInstructionArgs,
}

impl<'a, 'b> AddPerpMarketIndexCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_program::account_info::AccountInfo<'a>,
        accounts: AddPerpMarketIndexCpiAccounts<'a, 'b>,
        args: AddPerpMarketIndexInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            market_indexes: accounts.market_indexes,
            pricing: accounts.pricing,
            __args: args,
        }
    }

    #[inline(always)]
    pub fn invoke(&self) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }

    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }

    #[inline(always)]
    pub fn invoke_signed(
        &self,
        signers_seeds: &[&[&[u8]]],
    ) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }

    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> solana_program::entrypoint::ProgramResult {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_program::instruction::AccountMeta::new(
            *self.market_indexes.key,
            false,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new(
            *self.pricing.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_program::instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = borsh::to_vec(&AddPerpMarketIndexInstructionData::new()).unwrap();
        let mut args = borsh::to_vec(&self.__args).unwrap();
        data.append(&mut args);

        let instruction = solana_program::instruction::Instruction {
            program_id: crate::ZETA_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(3 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.market_indexes.clone());
        account_infos.push(self.pricing.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_program::program::invoke(&instruction, &account_infos)
        } else {
            solana_program::program::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `AddPerpMarketIndex` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` market_indexes
///   1. `[writable]` pricing
#[derive(Clone, Debug)]
pub struct AddPerpMarketIndexCpiBuilder<'a, 'b> {
    instruction: Box<AddPerpMarketIndexCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> AddPerpMarketIndexCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_program::account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(AddPerpMarketIndexCpiBuilderInstruction {
            __program: program,
            market_indexes: None,
            pricing: None,
            asset: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }

    #[inline(always)]
    pub fn market_indexes(
        &mut self,
        market_indexes: &'b solana_program::account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.market_indexes = Some(market_indexes);
        self
    }

    #[inline(always)]
    pub fn pricing(
        &mut self,
        pricing: &'b solana_program::account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.pricing = Some(pricing);
        self
    }

    #[inline(always)]
    pub fn asset(&mut self, asset: Asset) -> &mut Self {
        self.instruction.asset = Some(asset);
        self
    }

    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_program::account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }

    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }

    #[inline(always)]
    pub fn invoke(&self) -> solana_program::entrypoint::ProgramResult { self.invoke_signed(&[]) }

    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(
        &self,
        signers_seeds: &[&[&[u8]]],
    ) -> solana_program::entrypoint::ProgramResult {
        let args = AddPerpMarketIndexInstructionArgs {
            asset: self.instruction.asset.clone().expect("asset is not set"),
        };
        let instruction = AddPerpMarketIndexCpi {
            __program: self.instruction.__program,

            market_indexes: self
                .instruction
                .market_indexes
                .expect("market_indexes is not set"),

            pricing: self.instruction.pricing.expect("pricing is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct AddPerpMarketIndexCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_program::account_info::AccountInfo<'a>,
    market_indexes: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    pricing: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    asset: Option<Asset>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(
        &'b solana_program::account_info::AccountInfo<'a>,
        bool,
        bool,
    )>,
}
