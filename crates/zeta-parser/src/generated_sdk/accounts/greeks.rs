//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::{BorshDeserialize, BorshSerialize};
use solana_program::pubkey::Pubkey;

use crate::generated::types::{AnchorDecimal, ProductGreeks};

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct Greeks {
    pub discriminator: [u8; 8],
    pub nonce: u8,
    #[cfg_attr(feature = "serde", serde(with = "serde_big_array::BigArray"))]
    pub mark_prices: [u64; 46],
    #[cfg_attr(feature = "serde", serde(with = "serde_big_array::BigArray"))]
    pub mark_prices_padding: [u64; 91],
    pub perp_mark_price: u64,
    pub product_greeks: [ProductGreeks; 22],
    #[cfg_attr(feature = "serde", serde(with = "serde_big_array::BigArray"))]
    pub product_greeks_padding: [ProductGreeks; 44],
    pub update_timestamp: [u64; 2],
    pub update_timestamp_padding: [u64; 4],
    pub retreat_expiration_timestamp: [u64; 2],
    pub retreat_expiration_timestamp_padding: [u64; 4],
    pub interest_rate: [i64; 2],
    pub interest_rate_padding: [i64; 4],
    pub nodes: [u64; 5],
    pub volatility: [u64; 10],
    pub volatility_padding: [u64; 20],
    #[cfg_attr(feature = "serde", serde(with = "serde_big_array::BigArray"))]
    pub node_keys: [Pubkey; 138],
    pub halt_force_pricing: [bool; 6],
    pub perp_update_timestamp: u64,
    pub perp_funding_delta: AnchorDecimal,
    pub perp_latest_funding_rate: AnchorDecimal,
    pub perp_latest_midpoint: u64,
    #[cfg_attr(feature = "serde", serde(with = "serde_with::As::<serde_with::Bytes>"))]
    pub padding: [u8; 1593],
}

impl Greeks {
    pub const LEN: usize = 10240;

    #[inline(always)]
    pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
        let mut data = data;
        Self::deserialize(&mut data)
    }
}

impl<'a> TryFrom<&solana_program::account_info::AccountInfo<'a>> for Greeks {
    type Error = std::io::Error;

    fn try_from(
        account_info: &solana_program::account_info::AccountInfo<'a>,
    ) -> Result<Self, Self::Error> {
        let mut data: &[u8] = &(*account_info.data).borrow();
        Self::deserialize(&mut data)
    }
}

#[cfg(feature = "fetch")]
pub fn fetch_greeks(
    rpc: &solana_client::rpc_client::RpcClient,
    address: &solana_program::pubkey::Pubkey,
) -> Result<crate::shared::DecodedAccount<Greeks>, std::io::Error> {
    let accounts = fetch_all_greeks(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_greeks(
    rpc: &solana_client::rpc_client::RpcClient,
    addresses: &[solana_program::pubkey::Pubkey],
) -> Result<Vec<crate::shared::DecodedAccount<Greeks>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<Greeks>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        let account = accounts[i].as_ref().ok_or(std::io::Error::new(
            std::io::ErrorKind::Other,
            format!("Account not found: {}", address),
        ))?;
        let data = Greeks::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::DecodedAccount {
            address,
            account: account.clone(),
            data,
        });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_greeks(
    rpc: &solana_client::rpc_client::RpcClient,
    address: &solana_program::pubkey::Pubkey,
) -> Result<crate::shared::MaybeAccount<Greeks>, std::io::Error> {
    let accounts = fetch_all_maybe_greeks(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_greeks(
    rpc: &solana_client::rpc_client::RpcClient,
    addresses: &[solana_program::pubkey::Pubkey],
) -> Result<Vec<crate::shared::MaybeAccount<Greeks>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<Greeks>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        if let Some(account) = accounts[i].as_ref() {
            let data = Greeks::from_bytes(&account.data)?;
            decoded_accounts.push(crate::shared::MaybeAccount::Exists(
                crate::shared::DecodedAccount {
                    address,
                    account: account.clone(),
                    data,
                },
            ));
        } else {
            decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
        }
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountDeserialize for Greeks {
    fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
    }
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountSerialize for Greeks {}

#[cfg(feature = "anchor")]
impl anchor_lang::Owner for Greeks {
    fn owner() -> Pubkey { crate::ZETA_ID }
}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::IdlBuild for Greeks {}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::Discriminator for Greeks {
    const DISCRIMINATOR: [u8; 8] = [0; 8];
}
