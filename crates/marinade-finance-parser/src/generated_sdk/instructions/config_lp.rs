//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::{BorshDeserialize, BorshSerialize};

use crate::generated::types::Fee;

/// Accounts.
#[derive(Debug)]
pub struct ConfigLp {
    pub state: solana_program::pubkey::Pubkey,

    pub admin_authority: solana_program::pubkey::Pubkey,
}

impl ConfigLp {
    pub fn instruction(
        &self,
        args: ConfigLpInstructionArgs,
    ) -> solana_program::instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }

    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: ConfigLpInstructionArgs,
        remaining_accounts: &[solana_program::instruction::AccountMeta],
    ) -> solana_program::instruction::Instruction {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_program::instruction::AccountMeta::new(
            self.state, false,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new_readonly(
            self.admin_authority,
            true,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = borsh::to_vec(&ConfigLpInstructionData::new()).unwrap();
        let mut args = borsh::to_vec(&args).unwrap();
        data.append(&mut args);

        solana_program::instruction::Instruction {
            program_id: crate::MARINADE_FINANCE_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ConfigLpInstructionData {
    discriminator: [u8; 8],
}

impl ConfigLpInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [10, 24, 168, 119, 86, 48, 225, 17],
        }
    }
}

impl Default for ConfigLpInstructionData {
    fn default() -> Self { Self::new() }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ConfigLpInstructionArgs {
    pub min_fee: Option<Fee>,
    pub max_fee: Option<Fee>,
    pub liquidity_target: Option<u64>,
    pub treasury_cut: Option<Fee>,
}

/// Instruction builder for `ConfigLp`.
///
/// ### Accounts:
///
///   0. `[writable]` state
///   1. `[signer]` admin_authority
#[derive(Clone, Debug, Default)]
pub struct ConfigLpBuilder {
    state: Option<solana_program::pubkey::Pubkey>,
    admin_authority: Option<solana_program::pubkey::Pubkey>,
    min_fee: Option<Fee>,
    max_fee: Option<Fee>,
    liquidity_target: Option<u64>,
    treasury_cut: Option<Fee>,
    __remaining_accounts: Vec<solana_program::instruction::AccountMeta>,
}

impl ConfigLpBuilder {
    pub fn new() -> Self { Self::default() }

    #[inline(always)]
    pub fn state(&mut self, state: solana_program::pubkey::Pubkey) -> &mut Self {
        self.state = Some(state);
        self
    }

    #[inline(always)]
    pub fn admin_authority(
        &mut self,
        admin_authority: solana_program::pubkey::Pubkey,
    ) -> &mut Self {
        self.admin_authority = Some(admin_authority);
        self
    }

    /// `[optional argument]`
    #[inline(always)]
    pub fn min_fee(&mut self, min_fee: Fee) -> &mut Self {
        self.min_fee = Some(min_fee);
        self
    }

    /// `[optional argument]`
    #[inline(always)]
    pub fn max_fee(&mut self, max_fee: Fee) -> &mut Self {
        self.max_fee = Some(max_fee);
        self
    }

    /// `[optional argument]`
    #[inline(always)]
    pub fn liquidity_target(&mut self, liquidity_target: u64) -> &mut Self {
        self.liquidity_target = Some(liquidity_target);
        self
    }

    /// `[optional argument]`
    #[inline(always)]
    pub fn treasury_cut(&mut self, treasury_cut: Fee) -> &mut Self {
        self.treasury_cut = Some(treasury_cut);
        self
    }

    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: solana_program::instruction::AccountMeta,
    ) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }

    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_program::instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }

    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_program::instruction::Instruction {
        let accounts = ConfigLp {
            state: self.state.expect("state is not set"),
            admin_authority: self.admin_authority.expect("admin_authority is not set"),
        };
        let args = ConfigLpInstructionArgs {
            min_fee: self.min_fee.clone(),
            max_fee: self.max_fee.clone(),
            liquidity_target: self.liquidity_target.clone(),
            treasury_cut: self.treasury_cut.clone(),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `config_lp` CPI accounts.
pub struct ConfigLpCpiAccounts<'a, 'b> {
    pub state: &'b solana_program::account_info::AccountInfo<'a>,

    pub admin_authority: &'b solana_program::account_info::AccountInfo<'a>,
}

/// `config_lp` CPI instruction.
pub struct ConfigLpCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_program::account_info::AccountInfo<'a>,

    pub state: &'b solana_program::account_info::AccountInfo<'a>,

    pub admin_authority: &'b solana_program::account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: ConfigLpInstructionArgs,
}

impl<'a, 'b> ConfigLpCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_program::account_info::AccountInfo<'a>,
        accounts: ConfigLpCpiAccounts<'a, 'b>,
        args: ConfigLpInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            state: accounts.state,
            admin_authority: accounts.admin_authority,
            __args: args,
        }
    }

    #[inline(always)]
    pub fn invoke(&self) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }

    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }

    #[inline(always)]
    pub fn invoke_signed(
        &self,
        signers_seeds: &[&[&[u8]]],
    ) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }

    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> solana_program::entrypoint::ProgramResult {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_program::instruction::AccountMeta::new(
            *self.state.key,
            false,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new_readonly(
            *self.admin_authority.key,
            true,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_program::instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = borsh::to_vec(&ConfigLpInstructionData::new()).unwrap();
        let mut args = borsh::to_vec(&self.__args).unwrap();
        data.append(&mut args);

        let instruction = solana_program::instruction::Instruction {
            program_id: crate::MARINADE_FINANCE_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(3 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.state.clone());
        account_infos.push(self.admin_authority.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_program::program::invoke(&instruction, &account_infos)
        } else {
            solana_program::program::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `ConfigLp` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` state
///   1. `[signer]` admin_authority
#[derive(Clone, Debug)]
pub struct ConfigLpCpiBuilder<'a, 'b> {
    instruction: Box<ConfigLpCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> ConfigLpCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_program::account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(ConfigLpCpiBuilderInstruction {
            __program: program,
            state: None,
            admin_authority: None,
            min_fee: None,
            max_fee: None,
            liquidity_target: None,
            treasury_cut: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }

    #[inline(always)]
    pub fn state(&mut self, state: &'b solana_program::account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.state = Some(state);
        self
    }

    #[inline(always)]
    pub fn admin_authority(
        &mut self,
        admin_authority: &'b solana_program::account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.admin_authority = Some(admin_authority);
        self
    }

    /// `[optional argument]`
    #[inline(always)]
    pub fn min_fee(&mut self, min_fee: Fee) -> &mut Self {
        self.instruction.min_fee = Some(min_fee);
        self
    }

    /// `[optional argument]`
    #[inline(always)]
    pub fn max_fee(&mut self, max_fee: Fee) -> &mut Self {
        self.instruction.max_fee = Some(max_fee);
        self
    }

    /// `[optional argument]`
    #[inline(always)]
    pub fn liquidity_target(&mut self, liquidity_target: u64) -> &mut Self {
        self.instruction.liquidity_target = Some(liquidity_target);
        self
    }

    /// `[optional argument]`
    #[inline(always)]
    pub fn treasury_cut(&mut self, treasury_cut: Fee) -> &mut Self {
        self.instruction.treasury_cut = Some(treasury_cut);
        self
    }

    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_program::account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }

    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }

    #[inline(always)]
    pub fn invoke(&self) -> solana_program::entrypoint::ProgramResult { self.invoke_signed(&[]) }

    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(
        &self,
        signers_seeds: &[&[&[u8]]],
    ) -> solana_program::entrypoint::ProgramResult {
        let args = ConfigLpInstructionArgs {
            min_fee: self.instruction.min_fee.clone(),
            max_fee: self.instruction.max_fee.clone(),
            liquidity_target: self.instruction.liquidity_target.clone(),
            treasury_cut: self.instruction.treasury_cut.clone(),
        };
        let instruction = ConfigLpCpi {
            __program: self.instruction.__program,

            state: self.instruction.state.expect("state is not set"),

            admin_authority: self
                .instruction
                .admin_authority
                .expect("admin_authority is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct ConfigLpCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_program::account_info::AccountInfo<'a>,
    state: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    admin_authority: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    min_fee: Option<Fee>,
    max_fee: Option<Fee>,
    liquidity_target: Option<u64>,
    treasury_cut: Option<Fee>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(
        &'b solana_program::account_info::AccountInfo<'a>,
        bool,
        bool,
    )>,
}
