[package]
name = "shipstern-stream"
description = "A gRPC server for subscribing to parsed Yellowstone events"
version.workspace = true
# authors = ["TODO"]
edition.workspace = true
repository.workspace = true
authors.workspace = true
license.workspace = true
readme = "./../../README.md"

[dependencies]
async-trait = { workspace = true }
clap = { workspace = true, features = [
  "env",
  "derive",
  "std",
] }
futures-channel = { workspace = true, features = ["sink"] }
futures-util = { workspace = true, features = ["sink"] }
pin-project-lite = { workspace = true }
prometheus = { workspace = true, features = ["push"], optional = true }
toml = { workspace = true }
serde = { workspace = true, features = ["derive"] }
smallvec = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true, features = ["rt-multi-thread", "signal"] }
tracing = { workspace = true }
yellowstone-grpc-client = { workspace = true }
shipstern-core = { workspace = true }
shipstern = { workspace = true }
shipstern-yellowstone-grpc-source = { workspace = true }
shipstern-proto = { workspace = true, features = ["stream"] }

[features]
default = []
prometheus = ["dep:prometheus", "shipstern/prometheus"]
