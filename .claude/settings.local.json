{
  "permissions": {
    "allow": [
      "Bash(cargo check:*)",
      "Bash(cargo test:*)",
      "Bash(find:*)",
      "Bash(cargo fmt:*)",
      "Bash(cargo clippy:*)",
      "Bash(RUST_LOG=warn cargo test --package yellowstone-vixen-integration-tests --test integration_test -- --show-output test_okx_specific_signatures)"
    ],
    "deny": [],
    "ask": []
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$FILE_PATH\" | grep -q '\\.rs$'; then cargo +nightly fmt --all && cargo clippy --fix --allow-dirty --allow-staged; fi",
            "statusMessage": "Running cargo fmt and clippy..."
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$FILE_PATH\" | grep -q '\\.rs$'; then cargo +nightly fmt --all && cargo clippy --fix --allow-dirty --allow-staged; fi",
            "statusMessage": "Running cargo fmt and clippy..."
          }
        ]
      }
    ]
  }
}